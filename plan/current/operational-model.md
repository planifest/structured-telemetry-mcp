# Operational Model - 0000010-macos-launchd-service

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000010-macos-launchd-service
**Version:** 0.10.0

> Local developer-tool operational model — there is no production fleet, on-call rotation, or centralized alerting. This documents the equivalent local-first practices.

---

## Component Ownership

| Component | Owner (team/role) | On-call Rotation | Escalation Path |
|-----------|------------------|-----------------|-----------------|
| structured-telemetry-mcp | the developer running it locally | none — single-user local tool | n/a — self-service via `service:status` / logs |

---

## Runbook Triggers

| Trigger | Condition | Action | Automated? |
|---------|-----------|--------|-----------|
| Service not responding | `/health` fails to respond after install/restart | Check `launchctl list` / `systemctl --user status`, then log files | no |
| Repeated crash-restart | `launchctl print` / `journalctl --user -u planifest-telemetry-mcp` shows rapid restart cycling | Inspect stderr log for the crash cause; do not disable `KeepAlive`/`Restart` as a workaround | no |
| Lingering warning shown at install (Linux) | `loginctl show-user $USER --property=Linger` != `yes` | Developer runs the printed `loginctl enable-linger $USER` command if they want post-logout survival | no — explicit human action by design (req-007) |
| `emit_event` rejected with a Zod error | Malformed envelope from a calling agent | Read the Zod error (now specific, e.g. "expected object, received string") and fix the caller — this is the intended, improved failure mode from req-009/req-010 | no |

---

## Alerting Thresholds

Not applicable — no centralized monitoring/alerting system for this local dev tool. `service:status` (req-002, req-006) is the manual equivalent of a health check.

---

## Deployment Model

| Component | Strategy | Rollback Plan | Health Check |
|-----------|----------|--------------|-------------|
| structured-telemetry-mcp (macOS) | `service:install` writes + bootstraps a LaunchAgent; re-running `install` is idempotent (bootout before bootstrap) | `service:uninstall` removes the plist and unloads it; reinstall the prior build if needed | `GET http://localhost:3741/health` |
| structured-telemetry-mcp (Linux) | `service:install` writes + enables a `systemd --user` unit; re-running `install` is idempotent | `service:uninstall` disables and removes the unit | `GET http://localhost:3741/health` + `systemctl --user is-active` |

---

## Backup and Recovery

| Data Store | Backup Frequency | Retention | Recovery Time Objective | Recovery Point Objective |
|-----------|-----------------|-----------|------------------------|------------------------|
| DuckDB (`~/.planifest/telemetry.db`) | none — unchanged from 0000008's established posture; this feature does not alter backup policy | n/a | n/a — local dev telemetry, not business-critical data | n/a |

---

*Generated by spec-agent. Path: `plan/current/operational-model.md`*
