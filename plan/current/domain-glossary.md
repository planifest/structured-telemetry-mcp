---
title: "Domain Glossary - 0000010-macos-launchd-service"
summary: "Definitions of domain terms used within this feature."
status: "active"
version: "0.1.0"
---
# Domain Glossary - 0000010-macos-launchd-service

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md) (updated by any agent that introduces a new domain term)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000010-macos-launchd-service
**Version:** 0.10.0

> The ubiquitous language for this feature. Agents and humans use these terms — in code, comments, file names, variable names, and documentation.

---

## Terms

| Term | Definition | Aliases | Used In |
|------|-----------|---------|---------|
| LaunchAgent | A macOS `launchd`-managed process definition scoped to a single user (as opposed to a LaunchDaemon, which is system-wide). This feature installs a user LaunchAgent, never a LaunchDaemon. | user agent (informal) | structured-telemetry-mcp |
| `gui/$(id -u)` domain | The modern `launchctl` target domain for a logged-in user's GUI session, used with `bootstrap`/`bootout`/`enable` — replaces the deprecated `launchctl load -w` mechanism. | GUI domain | structured-telemetry-mcp |
| systemd user unit | A `systemd --user` service definition, running under a per-user systemd instance rather than the system-wide PID 1 instance — no root required to install or manage. | user service | structured-telemetry-mcp |
| Lingering | A per-user systemd setting (`loginctl enable-linger`) that keeps a user's `systemd --user` instance running after their last login session ends, so user services survive SSH/GUI logout. | linger | structured-telemetry-mcp |
| Envelope | The full JSON object passed to `emit_event`, containing the common fields (`schema_version`, `event`, `session_id`, `phase`, etc.) plus the typed `data` payload. Distinct from the envelope's own `event` field (the discriminator naming which event type this is) — the tool argument is renamed to `envelope` (req-012) specifically to avoid this name collision. | event envelope | structured-telemetry-mcp |
| Event type | The value of an envelope's `event` field — one of 25 enumerated strings (21 existing + 4 added by req-011) identifying what kind of telemetry record this is (e.g. `phase_start`, `loop_iteration`). | — | structured-telemetry-mcp |
| Tool argument schema | The Zod (and derived JSON Schema) definition an MCP server registers for a tool's input — what a calling model sees when deciding how to construct a tool call. Distinct from the wire-level validation (`validateEvent()`/ajv) applied after the call is received. | argument shape, input schema | structured-telemetry-mcp |
| Loop iteration | One pass through a governed pipeline loop (`p0_completeness`, `design_critic`, `reversal_protocol`, `verify_by_execution`, `cross_model_review`), recorded via the `loop_iteration` event type (req-011). | — | structured-telemetry-mcp, planifest-framework |
| Phase reversal | A governed, budget-limited correction of an upstream P0–P6 artifact, petitioned by a blocked phase agent and granted or denied by a fresh-context assessor. Recorded via `phase_reversal_petitioned`/`_granted`/`_denied` (req-011). | reversal protocol | structured-telemetry-mcp, planifest-framework |
| Cascade size | The count of downstream artifacts invalidated and requiring re-work when a phase reversal is granted — a field on `phase_reversal_granted`/`_denied` events. | — | structured-telemetry-mcp, planifest-framework |

---

*Generated by spec-agent. Updated by any agent that introduces a new domain term. Path: `plan/current/domain-glossary.md`*
