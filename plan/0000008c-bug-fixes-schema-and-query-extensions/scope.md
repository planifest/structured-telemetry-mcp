---
title: "Scope - 0000008c-mcp-fixes-and-enhancements"
summary: "Explicit boundaries for the 0008c patch release."
status: "active"
version: "0.1.0"
---
# Scope - 0000008c-mcp-fixes-and-enhancements

**Skill:** spec-agent
**Tool:** Claude Code
**Model:** claude-sonnet-4-6
**Feature:** 0000008c-mcp-fixes-and-enhancements
**Phase:** 1
**Version:** 0.1.0

---

## In Scope

- **SCH-001** Add `phase_skip` event type to `schemas/telemetry-event.schema.json`
- **SCH-002** Add `security_finding` event type (with `severity: "critical"` as a new enum value)
- **SCH-003** Add `retry_limit_exceeded` event type
- **SCH-004** Add `adr_decision` event type
- **SCH-005** Add `doc_gap` event type
- **BUG-001** Add `mcp_mode` to `BottleneckGroupBy` and `resolveGroupColumn()` in `src/query/bottlenecks.ts`
- **BUG-002** Validate `session_id` presence for `failure_sequence` in `src/query/failures.ts`; throw on missing or empty
- **BUG-003** Validate `session_id` presence for `drill_down` in `src/query/token-efficiency.ts`; throw on missing or empty
- **FEA-001** Add `mode: "event_log"` query — full raw event stream scoped by `session_id` and/or `initiative_id`, ordered by timestamp, with `limit` support
- **FEA-002** Add `group_by: "initiative_id"` to `BottleneckGroupBy` (with `COALESCE` for NULL handling)
- **FEA-003** Add optional `initiative_id` filter to all three query families (`bottlenecks`, `failures`, `token-efficiency`)
- **POST-001** `scripts/DELETE-ALL-PRODUCTION-RECORDS.ps1` and `.sh` — human-only, admin/sudo-gated, interactive-phrase-confirmed truncation scripts
- **TypeScript types** Update `src/types/events.ts` union type to include 5 new event discriminants
- **Data contract** Update `src/structured-telemetry-mcp/docs/data-contract.md` to reflect 14 event types and add missing `model_config` column documentation
- **Component manifest** Bump `src/structured-telemetry-mcp/component.yml` version to `0.2.0`
- **Dependency verification** Confirm all npm dependencies are at latest stable versions against live registry before release build
- **Version bump** `package.json` version `0.1.0` → `0.2.0`
- **54 new tests** across `tests/unit/validation.test.ts`, `tests/unit/server-factory.test.ts`, `tests/integration/query-telemetry.test.ts`

---

## Out of Scope

- 0008b framework skill documentation updates (tracked separately in the framework repo)
- Authentication or access control on the HTTP/SSE endpoint
- DuckDB structural schema changes (no DDL modifications; new event types use existing `data` JSON column)
- npm publish to registry
- Pagination for `event_log` beyond the `limit` parameter
- Any query family returning unbounded results (no `limit` → default 100 rows for `event_log`)
- Changes to the MCP transport, HTTP/SSE server, or Express middleware
- Changes to `src/cli.ts` (no new subcommands — truncation is shell-script only)
- Windows Service (`deploy.ps1`, `service.ps1`) changes

---

## Deferred

Nothing deferred.

---

*Generated by spec-agent. Path: `plan/current/scope.md`*
