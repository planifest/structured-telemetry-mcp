# Test Report — 0000010-macos-launchd-service — 2026-07-19

**Feature:** macOS + Linux Background Service + emit_event Envelope Fix
**Plan date:** 2026-07-19
**Pipeline run:** P0 Assess → P1 Spec → P2 ADRs → P3 Codegen → P4 Validate → P5 Security → P6 Docs → P7 Archive

> Note: this template's §2 "Regression Pack State" refers to `planifest-framework/tests/regression/` (the framework's own dev test suite) — not applicable to this consuming repo. Adapted below to this repo's own `tests/regression/` directory, which serves the equivalent role here.

---

## 1. Tests Run This Plan (P4 Results)

| Test file | Requirement ID(s) | Status |
|-----------|-------------------|--------|
| `tests/unit/server-factory.test.ts` | req-009 | pass |
| `tests/regression/emit-handler.test.ts` | req-010, req-012 | pass |
| `tests/regression/event-types.test.ts` | req-011 | pass |
| `tests/regression/cross-field-validation.test.ts` | req-011 | pass |
| `tests/integration/emit-event.test.ts` | req-009, req-011 | pass |
| `scripts/service-macos.sh` (manual verification only) | req-001, req-002, req-003 | not automated — see `src/structured-telemetry-mcp/docs/quirks.md` |
| `scripts/service-linux.sh` (manual verification only, **untested on real hardware**) | req-005, req-006, req-007 | not automated, not yet manually verified — risk-register R-002 |
| README.md / usage-guide.md (doc review only) | req-004, req-008, req-012 | reviewed |

**Summary:** 318 tests run — 318 passed, 0 failed, 0 skipped. `req-001`–`req-008` (bash/plist/systemd) are covered by manual verification per `plan/current/design.md`'s declared testing strategy, not by this table's automated pass/fail — no shell-script test harness exists in this repo (documented deviation, `quirks.md`).

Every requirement in `plan/current/requirements/` is accounted for above.

---

## 2. Regression Pack State (this repo's `tests/regression/`, adapted from the template's framework-repo default)

**Total tests in `tests/regression/`:** 135 (as of this feature; includes both pre-existing and this feature's additions)
**Passed:** 135
**Failed:** 0

No `# REGRESSION-CANDIDATE:` tags were found in any test file written this feature (scanned across `tests/` — none present). No promotions to consider.

> No tests were promoted this feature — all new tests in `tests/regression/` were written directly as regression tests from the start (this repo's existing convention per `tests/regression/`'s per-file structure), not via a separate candidate→promotion step.

---

## 3. Newly Promoted Tests (This Feature)

> No tests were promoted to the regression pack for this feature — see §2.

---

## 4. Summary

| Metric | Value |
|--------|-------|
| Total tests run (P4) | 318 |
| Pass rate (P4) | 100% |
| Regression pack size (`tests/regression/`) | 135 tests |
| Regression pass rate | 100% |
| Newly promoted tests | 0 |
| Regression failures blocking ship | 0 |

**Overall test health:** ✅ Healthy — all automated tests passing. One manual-verification item remains open (Linux service script untested on real hardware, risk-register R-002) — tracked, not blocking this repo's own CI/ship gate per its declared testing strategy, but flagged prominently for follow-up.

---

*Generated by planifest-ship-agent at P7. Template: `planifest-framework/templates/test-report.template.md`*
