---
title: "ADR-002: Common envelope shape for hook script inputs"
summary: "All Planifest hook scripts receive a standardised JSON envelope {session_id, cwd, tool_input, event} regardless of which tool is running. Adapters translate tool-native payloads to this shape before delegation."
status: "accepted"
version: "0.1.0"
---
# ADR-002 - Common envelope shape for hook script inputs

**Skill:** [adr-agent](../../planifest-framework/skills/planifest-adr-agent/SKILL.md)
**Tool:** claude-code
**Model:** claude-sonnet-4-5
**Feature:** 0000003-hook-based-enforcement
**Component:** planifest-framework/hooks/
**Status:** accepted
**Date:** 2026-04-18

---

## Context

Each agentic tool passes a different JSON payload to its hook commands. Claude Code passes `{tool_name, tool_input, session_id, ...}`; Cursor passes a different shape; OpenCode's plugin API exposes event objects via TypeScript interfaces. The shared enforcement scripts (`gate-write.mjs`, `check-design.mjs`, `emit-phase-start.mjs`) need to read `cwd`, `session_id`, and the target file path from the hook input. If each script handles all possible tool payload shapes, the scripts become complex and tool-coupled. Any new tool requires changes to every shared script.

---

## Decision

Define a canonical common envelope that all shared scripts expect:

```json
{
  "session_id": "<string>",
  "cwd": "<absolute path to project root>",
  "tool_input": { "<tool-specific input fields>" },
  "event": "<hook event type e.g. PreToolUse>"
}
```

Adapter scripts (one per Tier 1/1b tool, the Tier 2 plugin) are responsible for translating the tool-native payload into this shape before calling the shared scripts. Shared scripts read only from the common envelope — they have no knowledge of tool-specific payload differences.

If `cwd` is absent in the common envelope, shared scripts fall back to `process.cwd()`. If `session_id` is absent, shared scripts fall back to the `.planifest-session` file (DD-001).

---

## Alternatives Considered

| Alternative | Pros | Cons | Why Rejected |
|-------------|------|------|-------------|
| Each shared script handles all tool payload shapes | No adapter layer | Scripts grow with every new tool; N tools × M scripts coupling | Maintenance burden grows quadratically; violates single-responsibility |
| Pass raw tool payload; scripts detect tool via env var | Simpler adapters | Scripts still need conditional logic per tool | Moves coupling into shared scripts, not out of them |
| Tool-specific script variants (e.g. `gate-write-cursor.mjs`) | Zero shared code complexity | M scripts × N tools = large duplication; bugs fixed in N places | Unsustainable duplication |
| Protocol buffer / schema-validated envelope | Strong typing, versioned contract | Heavy tooling for a shell script context; overkill | Node.js built-ins are the only dependency; protobuf not viable |

---

## Affected Components

| Component | Impact |
|-----------|--------|
| `planifest-framework/hooks/enforcement/gate-write.mjs` | Reads only from common envelope |
| `planifest-framework/hooks/enforcement/check-design.mjs` | Reads only from common envelope |
| `planifest-framework/hooks/telemetry/emit-phase-start.mjs` | Reads only from common envelope |
| `planifest-framework/hooks/telemetry/emit-phase-end.mjs` | Reads only from common envelope |
| `planifest-framework/hooks/adapters/*.mjs` | Each adapter owns the translation to common envelope |
| `planifest-framework/hooks/adapters/opencode/` | Plugin translates OpenCode event objects to common envelope |

---

## Consequences

**Positive:**
- Adding a new tool requires only a new adapter, not changes to any shared script
- Shared scripts can be tested with mock common envelope payloads independent of any tool
- The envelope shape is a public contract — tool adapter authors know exactly what to produce

**Negative:**
- Adapters add a translation layer that must be kept correct; a bug in an adapter silently passes wrong data to shared scripts
- The `cwd` field is critical for path resolution; if an adapter sets it incorrectly, gate-write.mjs will resolve `design.md` from the wrong root — produces false positives or false negatives

**Risks:**
- If a tool changes its native hook payload shape, the adapter must be updated. There is no runtime validation that the common envelope is correctly formed (only the adapter author's correctness). Consider adding a lightweight envelope validation step in shared scripts (log warning, do not block).

---

## Related ADRs

- ADR-001 - depends-on (common envelope is what makes the adapter pattern viable)

---

## Supersedes

- None

## Superseded By

- None

---

*Generated by adr-agent. Path: `plan/current/adr/ADR-002-common-envelope-shape.md`*
