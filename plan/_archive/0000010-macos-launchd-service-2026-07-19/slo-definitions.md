# SLO Definitions - 0000010-macos-launchd-service

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000010-macos-launchd-service
**Version:** 0.10.0

> Local single-user dev tool — no formal SLO/error-budget regime applies (carried forward from 0000008: "local process — no SLO applicable"). The measurable targets below are the closest equivalent, drawn directly from this feature's NFRs.

---

## Service Level Objectives

| SLO ID | Service / Component | SLI (what is measured) | Target | Window | Error Budget |
|--------|-------------------|----------------------|--------|--------|-------------|
| SLO-001 | structured-telemetry-mcp — `emit_event` | proportion of calls completing in < 5ms (p95) | p95 < 5ms | per test run (`tests/performance.test.ts`) | n/a — CI gate, not a rolling production budget |
| SLO-002 | structured-telemetry-mcp — macOS/Linux service | service reachable at `/health` within a few seconds of install/boot | 100% of manual verification runs (req-001, req-005 AC) | per install/reboot test | n/a — manual verification, not continuously monitored |

---

## SLI Definitions

| SLI ID | Name | Measurement Method | Data Source | Good Event Definition | Valid Event Definition |
|--------|------|-------------------|-------------|----------------------|----------------------|
| SLI-001 | `emit_event` latency | Wall-clock time from tool-call receipt to response, measured in `tests/performance.test.ts` | Vitest performance test output | < 5ms | any completed `emit_event` call in the test run |
| SLI-002 | Service reachability post-install | Retry-looped `curl http://localhost:3741/health` after install/restart | Manual test run per req-001/req-005 acceptance criteria | HTTP 200 within the retry window | any install/restart attempt |

---

## Error Budget Policy

Not applicable — this is a local developer tool with manual verification, not a continuously-monitored production service. No burn-rate alerting or budget-based change-freeze policy applies. If `SLO-001`'s CI gate fails, it blocks the P4 validate gate directly rather than accumulating against a rolling budget.

---

## Burn Rate Alerts

Not applicable — see Error Budget Policy above.

---

*Generated by spec-agent. Path: `plan/current/slo-definitions.md`*
