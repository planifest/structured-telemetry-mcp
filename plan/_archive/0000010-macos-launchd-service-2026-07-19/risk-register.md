---
title: "Risk Register - 0000010-macos-launchd-service"
summary: "Technical, operational, and security risks with their mitigations."
status: "active"
version: "0.1.0"
---
# Risk Register - 0000010-macos-launchd-service

**Skill:** [spec-agent](../../planifest-framework/skills/planifest-spec-agent/SKILL.md) (updated by any agent that identifies a new risk)
**Tool:** Claude Code
**Model:** claude-sonnet-5
**Feature:** 0000010-macos-launchd-service
**Version:** 0.10.0
**Overall Risk Level:** low

---

## Risks

| ID | Category | Description | Likelihood | Impact | Mitigation | Status |
|----|----------|------------|------------|--------|-----------|--------|
| R-001 | operational | `~/Library/LaunchAgents` root-owned by MDM/endpoint-security policy on some dev machines, blocking user-level plist writes | medium — already observed on one real dev machine | medium — install fails without the fallback path | req-003: pre-flight write-test + clear explanation + sudo-command fallback (never silent sudo) | open |
| R-002 | technical | Linux systemd unit file design (`linux-systemd-reference.md`) is entirely untested on real hardware — this session had no Linux machine available | medium | medium — Phase 2 acceptance criteria could fail during codegen/validate against a real distro | req-005: verify on at least one real systemd-based distro before shipping Phase 2 | open |
| R-003 | operational | `systemd --user` lingering disabled by default on many headless/dev/container boxes — service silently dies on SSH logout without warning | medium — common default, not an edge case | medium — surprising failure mode, discovered only when the backend is unexpectedly down | req-007: post-install + `status` lingering check with explicit warning and exact remediation command | open |
| R-004 | technical | The three `emit_event` enforcement points (Zod tool-argument enum, ajv schema `$defs`/enum, `EVENT_REQUIRED_DATA_FIELDS`) can drift out of sync if not updated together | low — explicit step-by-step sequencing in req-009/req-011 and the RCA spec's own Definition of Done | high — would silently re-break telemetry for a subset of event types, the exact failure mode (R-009) this fix exists to close | req-009, req-011: land all three together in one change; integration test asserts all 25 types round-trip end-to-end through the real handler | open |
| R-005 | technical | `emit_event` argument rename (`event` → `envelope`, req-012) is a breaking change for any caller still using the old shape | low — no known external callers besides `planifest-framework`, which will be updated to match | medium — a caller using the old argument name would get a clear Zod rejection rather than silent failure, but does require a coordinated update | req-012: version bump to 0.10.0 signals the breaking change; README/usage-guide updated in the same pass | open |
| R-006 | operational | Version-manifest drift (`package.json` vs `component.yml` vs git tags) already caused confusion once this session | low once corrected | low — cosmetic/confusing rather than functional; does not affect runtime behaviour | `component.yml`/`product.yml` established as the enforced version source this pass; `package.json` synced to match at ship time | open |
| R-007 | operational | Node binary path is not guaranteed on either platform (Homebrew Intel vs. Apple Silicon; `$PATH`/nvm/distro package on Linux) — a hardcoded path would break on a differently-configured machine | low — `mac-setup.md` §3 already establishes the dynamic-resolution pattern for `npm root -g`, being reused here | medium — service would fail to start with a confusing "file not found" if node path is wrong | req-001, req-005: resolve via `command -v node` at install time, substituted into the plist/unit file, never hardcoded | open |

---

## Assumptions Logged as Risks

| ID | Assumption | Impact if Wrong | Status |
|----|-----------|----------------|--------|
| A-001 | The backend continues to read `PLANIFEST_MCP_PORT` / `PLANIFEST_TELEMETRY_DB` from env with existing defaults — no plist/unit-level env override is required | Plist/unit files would need an `EnvironmentVariables`/`Environment=` block added | open |
| A-002 | No known caller currently sends `emit_event`'s argument as anything other than a plain object | The new Zod gate (req-009) would start rejecting a previously-"working" (by accident) caller that was silently serializing the envelope | open |
| A-003 | Phase 2 (Linux) targets systemd-based distros only; non-systemd init systems (Alpine/OpenRC, some minimal containers) are out of scope | A non-systemd target would need a different service-management approach entirely — explicitly not built this pass (req-005's `command -v systemctl` check fails cleanly rather than attempting a fallback) | open |

---

*Generated by spec-agent. Updated by any agent that identifies a new risk. Path: `plan/current/risk-register.md`*
